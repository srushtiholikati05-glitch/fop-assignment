#include<stdio.h>

int main(){
    int s[3];
    for(int i=0;i<3;i++){
        printf("Enter the number :");
        scanf("%d",&s[i]);
    }
    int a=0;
    for(int i=0;i<3;i++){
        a=a+(s[i]*s[i]*s[i]);
    }
    if(s[4]==a){
        printf("Given number %d is armstrong",a);
    }else{
        printf("GIven number %d is not armstrong",s);
    }
    return 0;
}
